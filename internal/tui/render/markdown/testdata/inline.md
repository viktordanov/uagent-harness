Run `go test` and **check** the *output*, then ~~ignore~~ the rest.
A soft break keeps its line, and a hard break\
ends one too. Nested: **bold with `code` and *italic* inside**.

Links: [the docs](https://example.com/docs), <https://example.com/auto>, and a bare https://github.com/yuin/goldmark.
Images: ![a diagram](img/diagram.png). Escapes: \*not italic\*, &amp; &copy; &#35;.
Words: snake_case_names and 2 * 3 * 4 stay as they are; a very_long_identifier_name_that_will_need_wrapping_somewhere too.
